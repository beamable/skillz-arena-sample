# Dependencies
node_modules/
.pnp/
.pnp.js

# React Native / Expo
.expo/
.expo-shared/
dist/
web-build/

# Metro / TypeScript / test output
.metro-cache/
.turbo/
.expo-logs/
coverage/
*.tsbuildinfo

# Native build output
android/.gradle/
android/app/build/
android/build/
ios/Pods/
ios/build/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# .NET / Beamable microservice build output
bin/
obj/
TestResults/
*.user
*.suo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment and local config
.env
.env.*
!.env.example
env.local
local.settings.json
appsettings.Development.json

# Beamable workspace config
.beamable/*
!.beamable/
!.beamable/config.beam.json
!.beamable/additional-project-paths.json
!.beamable/project-paths-to-ignore.json
# NOTE: .beamable/local is intentionally tracked for this sample so its content set ships
# with the repo (see README "Beamable Local Content"). In production, keep it gitignored.
# Auth tokens and CLI logs live under .beamable/temp and stay ignored via the rule above.
!.beamable/local/
beamable.local.json

# Moe private repo memory
.aiMoe/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
